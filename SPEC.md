# 保護犬猫ポータル — 仕様

更新日: 2026-08-24

全国の保護犬猫の譲渡情報を、1か所で検索できるようにする Web サービスの PoC です。団体ごとにばらばらなサイトで掲載されている現状を、集約検索と元サイトへの送客で補う構想です。**掲載データはすべて架空とし、実在の団体・動物の情報は使いません。**

## 位置づけと目的

- 収益化を目的にしません。「収集 → AI構造化 → 検索」の集約基盤が動くことをデモとして示します。
- TypeScript / React / Hono / AWS Lambda / CDK / Amplify Gen2 を一貫した構成で使います。機能の数より、CLI・API・画面が同じ処理経路の上に載っていることを優先します。
- 実データ（自治体の公示や愛護団体のサイト）の収集・転載は範囲外とします。権利確認の負担を避けるためです。
- 外部サービスは AWS だけで完結させます。Cloudflare や外部のマネージド DB は使いません。運用の手間と秘密情報の管理先を増やさないためです。

## できること

利用者向け:

1. **検索** — 「小型で子どもと暮らせる犬」のような自由文で探せます。AI が問い合わせ文から種別・地域・条件を読み取り、候補を確からしい順に返します。
2. **詳細と送客** — 詳細画面は要点と出典を示し、譲渡の申込みは必ず元サイトへ誘導します。
3. **一覧** — 「地域 × 種別」で絞った一覧を見られます。

団体向け:

4. **登録** — ログインした団体が写真と自由文メモを送ると、AI が「種別・推定年齢・性格・譲渡条件」などを構造化して掲載します。構造化は登録操作の中で同期に行います。LLM は数秒で返るため、初版では待ち時間を許容します。
5. **掲載管理** — 自分の掲載を一覧・編集・削除できます。

運用向け:

6. **CLI** — 見本掲載の構造化（`pets structure`）と投入（`pets seed`）をコマンドラインから実行できます。sandbox の DynamoDB へ書き込みます。検索を手元で確かめるときは、ローカルで起動した API（`npm run dev:api`）へ curl を投げます。

画面はスマートフォンと PC の両方に対応したレスポンシブな Web です。

## 後回しにする機能

次は初版に含めません。設計上の置き場所だけ決めておきます。

- **LINE 新着通知** — 保存した検索条件に合う子が載ったときに LINE で知らせる。追加時は LINE Messaging API の Webhook を Lambda で受け、保存条件は DynamoDB の同じテーブルへ置く。
- **埋め込みウィジェット** — 団体の既存サイトへ1行のタグで掲載一覧を埋め込む。
- **写真のアップロード** — 初版は写真を URL で指定する。S3 への直接アップロードは後から足す。
- **構造化の非同期化（SQS）** — 登録件数が増えて同期処理が待たせるようになったら SQS を挟む。
- **一覧ページの静的生成** — 実データ運用で検索エンジン流入を狙う段階になったら行う。
- **クローラ（収集）** — 架空の公示ページを収集して AI で構造化する経路。初版のデータは seed で直接作るため不要。

## データ

- 掲載データは架空とする。団体、犬猫のプロフィール、掲載元ページのすべてを架空に作る。
- 写真は権利上安全なものだけを使う（自前撮影、生成画像、CC0 素材）。見本掲載の写真は生成画像を `public/pets/` へ置く。素材を作る道具は本サービスの範囲外とし、別リポジトリで持つ。
- 初期データは50件の**見本掲載**とする。ランダム生成はしない。全件が内容を固定した架空の掲載で、写真・呼び名・紹介文を別リポジトリ（`pets-image`）で作る。id は `sample-` で始める。
- 紹介文から検索用の項目（年齢区分・体格・相性・要約など）を作るのは、団体の登録と同じ LLM の構造化とする。ただし一度だけ実行し、結果を `data/listings.json` へ固定してリポジトリへ置く。掲載の内容が実行のたびに揺れると、検索の確認にならないためである。
- `pets seed` は `data/listings.json` を読んで DynamoDB へ投入する。同じ id は上書きし、再実行しても件数が増えない。
- 実在サイトへはアクセスしない。

### 掲載1件の項目

AI が構造化する項目と、収集・登録で決まる項目を分けて持ちます。

| 項目 | 内容 | 決まり方 |
|---|---|---|
| id | 掲載の識別子 | 生成 |
| species | `dog` / `cat` | AI |
| name | 呼び名（架空） | AI |
| sex | `male` / `female` / `unknown` | AI |
| ageCategory | `baby` / `young` / `adult` / `senior` / `unknown` | AI（推定） |
| size | `small` / `medium` / `large` / `unknown`（猫は `unknown` でよい） | AI（推定） |
| prefecture | 都道府県 | AI または団体の所在地 |
| traits | 性格を表す短い語句の配列 | AI |
| conditions | 譲渡条件の配列 | AI |
| suitability | 子ども・他の犬・猫・集合住宅との相性。各 `yes` / `no` / `unknown` | AI（推定） |
| summary | 検索結果に出す1〜2文の要約 | AI |
| description | 元の紹介文・メモ | 収集・登録 |
| organization | 団体の id・名前・サイト URL | 収集・登録 |
| photoUrl | 写真の URL | 収集・登録 |
| sourceUrl | 掲載元ページの URL | 収集・登録 |
| status | `available` / `adopted` / `closed` | 団体の操作 |
| source | `crawl` / `org` / `seed` | 収集・登録・seed |
| createdAt / updatedAt | ISO 8601 の日時 | 生成 |

AI の推定項目は、元の文章に根拠がなければ `unknown` を入れます。推定で埋めて断定しないためです。

## 技術構成

本節は目標の構成です。構成図と現在の実装状況は [README.md](README.md) を参照してください。

フロントエンドは React + Vite + TypeScript で作り、AWS Amplify Hosting から配信します（実体は CloudFront + S3 のマネージド配信です）。

API は API Gateway + Lambda で、Lambda 上のルーティングには Hono（aws-lambda アダプタ）を使います。団体ログインは Amazon Cognito のユーザープール（メール + パスワード）です。ソーシャルログインは使いません。

構成管理は Amplify Gen2 + AWS CDK をリポジトリ内の正本とします。Amplify Gen2 が受け持つのは Auth（Cognito 構築）と Hosting です。加えて、DynamoDB・API・Bedrock 権限を定義するカスタム CDK スタックを束ねる傘の役割を持ちます。Amplify Data（AppSync/GraphQL + DynamoDB）は使いません。データ層を REST に保つためです。

実装言語は TypeScript です。

### データの置き場所

データベースは Amazon DynamoDB の1テーブルです。PostgreSQL や外部のマネージド DB は使いません。VPC が要らず、期限なしの無料枠に収まり、CDK では1つの construct で済むためです。

- テーブルは `pk` / `sk` の単一テーブル設計とする。掲載は `pk = LISTING#<id>`、`sk = META`。団体ごとの一覧には `orgId` をキーにした GSI を使う（GSI の Query は「実装の順序」4 で足す）。
- LLM 呼び出しの日次カウンタも同じテーブルへ置く（`pk = COUNTER#llm`、`sk = <日付>`）。
- CLI とローカル開発は sandbox の DynamoDB を既定の保存先にする（`amplify_outputs.json` からテーブル名を読む）。自動テストだけ、同じ保存インターフェースのメモリ内実装を使う。初版の保存操作は「全件読む・1件書く・1件消す」の3つに限定し、団体別の読み出しは順序 4 で加える。

### 検索の仕組み

Embedding とベクトル類似度検索は使いません。初版の50件でも、実データを想定した数万件でも、次の4段で足ります。

1. LLM が自由文を構造化条件（種別・都道府県・サイズ・年齢区分・相性・キーワード）へ変換する。
2. 保存先から全件を読み、構造化条件で絞り込む。該当項目が `unknown` の掲載は除外しない。
3. キーワードと紹介文の文字 3-gram の重なりで粗い順位を付け、上位 20 件程度に絞る。
4. LLM が上位候補を読み、確からしい順に並べ替えて理由を1行添える。

初版は50件なので、3 の絞り込みが効く場面は多くありません。件数が増えても LLM へ渡す量を一定に保つための段です。

AI（LLM）には Amazon Bedrock 上の GPT-5.6 Luna（以下 Luna）を使います。Luna は Converse API ではリージョン内推論に対応しておらず、クロスリージョン推論プロファイル（`global.openai.gpt-5.6-luna`）を名指しして呼びます。Lambda と同じ東京リージョンの `bedrock-runtime` から呼べ、実際の推論先は AWS が選びます。権限は Lambda の IAM ロールに、その推論プロファイルとアカウントの既定プロジェクトへの `bedrock:InvokeModel` を付けます。Structured outputs は非対応なので、JSON はプロンプトで指示してスキーマ検証します。LLM の代替（語彙ルールのスタブなど）は作りません。自動テストは Bedrock の実応答を一度記録して再生し、絞り込み・並べ替え・スキーマ検証など周辺のロジックだけを固定します。LLM 出力の品質は、代表入力を本物に流した結果で確認します。

### 実装の順序

各機能は、まず手元から実行できる形（CLI かローカルの API）で作り、Web UI をその上へ重ねます。検索は CLI を作らず、ローカルで起動した API へ curl を投げて確かめます。同じ処理を二重に持たないためです。

1. 見本掲載 — `pets structure` が紹介文を LLM で構造化して `data/listings.json` へ固定し、`pets seed` がそれを投入する
2. バックエンドを sandbox で — CDK で DynamoDB・HTTP API・Lambda（Hono）・Bedrock 権限と「費用と防御」を定義し、`npx ampx sandbox` で展開する。`pets seed` で投入し、`POST /search` が期待どおりの候補を返す
3. 利用者向け Web UI — 検索・詳細・一覧。手元の `npm run dev` が sandbox の API を使う
4. 団体向け — Cognito ログイン、登録（メモを Bedrock で構造化）・編集・削除、掲載管理画面
5. 公開 — git リモートへ push し、Amplify アプリにブランチを接続して Hosting へ
6. 「後回しにする機能」

## 費用と防御

無料枠に収まる構成を基本とします。以下の金額は 2026-08 時点の単価による見積りで、実測ではありません。

- DynamoDB はプロビジョンドモードの期限なし無料枠（25GB、読み書き各 25 ユニット）に収まります。
- Lambda と API Gateway は PoC の利用量なら無料枠内です。
- LLM 費用は従量です。Luna は1検索あたり $0.0005 未満で、構造化も1件あたり同程度です。1日 100 回の利用で月 $1.5 程度、1日 300 回でも月 $4 程度です。
- Amplify Hosting の無料枠は12ヶ月型です。期限後の費用が合わなければ、素の S3 + CloudFront 配信へ戻せる依存度に留めます。

従量課金の最悪ケースは「落ちる」ではなく「請求」です。bot に叩かれても最悪額が有界になるよう、次を CDK で定義します。

- **AI を呼ぶルートのスロットリング** — AI を呼ぶルート（初版は `POST /search`）は API Gateway 上に明示的なルートとして定義し（他は `/{proxy+}` で Hono へ委ねる）、ルート別設定で 0.1 rps 程度に絞る。これが硬い上限で、張り付かれても1日約 8,600 回（1日約 $4、月に換算して約 $130）で止まる。
- **LLM の日次上限** — DynamoDB のカウンタで1日 300 回程度を超えたら AI 経路を止める。普段の利用を守る柔らかい上限。
- **Lambda の reserved concurrency** — 2〜5 に固定する。0 にすれば全停止できる。
- **API 全体のスロットリング**、**robots.txt での巡回禁止**、**CloudWatch Logs の保持期間 7 日**。

CloudWatch アラームや SNS のメール通知は任意です。Cloudflare と AWS WAF は使いません。

## 決めていないこと

- サービス名とドメイン
