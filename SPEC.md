# 保護犬猫ポータル — 仕様

更新日: 2026-08-20

全国の保護犬猫の譲渡情報を、1か所で検索できるようにする Web サービスの PoC です。団体ごとにばらばらなサイトで掲載されている現状を、集約検索と元サイトへの送客で補う構想です。**掲載データはすべて架空とし、実在の団体・動物の情報は使いません。**

## 位置づけと目的

- 収益化を目的にしません。「収集 → AI構造化 → 検索」の集約基盤が動くことを、デモとして示します。
- 実データ（自治体の公示や愛護団体のサイト）の収集・転載は範囲外とします。権利確認の負担を避けるためです。実データへ切り替える場合は、その時点で扱いを別途決めます。

## できること

利用者向け:

1. **検索** — 「小型で子どもと暮らせる犬」のような自由文で探せます。AI が問い合わせ文から種別・地域・条件を読み取り、候補を確からしい順に返します。
2. **詳細と送客** — 詳細画面は要点と出典を示し、譲渡の申込みは必ず元サイトへ誘導します。
3. **新着通知** — 検索条件を保存しておくと、合致する子が載ったときに LINE で通知します。

団体向け:

4. **登録** — 写真と自由文メモを送るだけで掲載できます。AI が「種別・推定年齢・性格・譲渡条件」などを構造化する処理は、登録操作の裏で非同期に行います。
5. **掲載管理** — 自分の掲載を一覧・編集できる管理画面を持ちます。
6. **埋め込みウィジェット** — 団体の既存ホームページへ、1行のタグで自分の掲載一覧を埋め込めます。

画面はスマートフォンと PC の両方に対応したレスポンシブな Web です。

## データ

- 掲載データは架空とする。団体、犬猫のプロフィール、掲載元ページのすべてを架空に作る。
- 写真は権利上安全なものだけを使う（自前撮影、生成画像、CC0素材）。
- クローラの収集先は、リポジトリ内に置いた架空の公示ページ（fixture HTML）とする。実在サイトへはアクセスしない。
- デモ用の架空データセットは数百件規模を目安に用意する。

## 技術構成

フロントエンドは React + Vite + TypeScript で作り、AWS Amplify Hosting から配信します（実体は CloudFront + S3 のマネージド配信です）。「地域×条件」の一覧ページは静的生成できる作りにします。実データ運用へ進んだ場合に、検索エンジン経由の流入を受けるためです。LINE は検索の入口としては補助で、新着通知を主な役割とします。

データ収集は「クローラ → SQS → Lambda で AI 構造化 → PostgreSQL」のパイプラインにします。団体の登録も同じ構造化経路に載せます。API は API Gateway + Lambda で、Lambda 上のルーティングには Hono（aws-lambda アダプタ）を使います。団体ログインは Amazon Cognito です。

構成管理は Amplify Gen2 + AWS CDK をリポジトリ内の正本とします。Amplify Gen2 が受け持つのは Auth（Cognito 構築）と Hosting、およびカスタム CDK スタック（SQS・クローラ・API・Bedrock 権限）を束ねる傘までです。Amplify Data（AppSync/GraphQL + DynamoDB）は使いません。データ層を PostgreSQL + REST に保つためです。

データベースは PostgreSQL です。置き場所は Aurora Serverless v2（Aurora Data API）と、無料枠のあるマネージド PostgreSQL（Neon など）の二択で、まだ決めていません。開発中はローカルの Docker で PostgreSQL を動かすため、この決定はクラウドへ載せるときまで先送りできます。検索は PostgreSQL の全文検索とあいまい一致（pg_trgm 拡張）で行い、Embedding とベクトル類似度検索は使いません。全国でも掲載は数万件規模で、この方式で足ります。

AI（LLM）には Amazon Bedrock 上の GPT-5.6 Luna を使います。LINE 連携は LINE Messaging API の Webhook を Lambda で受けます。実装言語は TypeScript を主とし、必要な箇所では Python も使います。

各機能は共通ルールに従い、まず CLI として実装してから Web UI をその上へ重ねます。クローラ・構造化・検索を CLI で通すことが最初の縦切りです。

## 費用

無料枠に収まる構成を基本とします。

- データベースは Neon を選べば無料枠（ストレージ 0.5GB、月 100 CU 時間、5分無操作で自動停止）に収まる見込みです。テキスト主体の数万件はこの範囲です。
- 写真の保存（S3）は架空データの分だけで、無料枠に収まります。
- LLM 費用は従量です。構造化は1件500トークン程度なので、数百件規模なら $1 未満です。検索応答を合わせても月数ドルの見込みです。
- LINE の push 通知は無料枠（月200通）から始めます。超える場合は費用を確認してから有料プランを判断します。
- Amplify Hosting の無料枠は12ヶ月型です。期限後の費用が合わなければ、素の S3 + CloudFront 配信へ戻せる依存度に留めます。

## 決めていないこと

- サービス名とドメイン
- データベースの置き場所（Aurora か Neon か）
