# AGENTS.md — PoCs リポジトリの規則

このリポジトリの規則の正本はこのファイルとする。共通作業ルール（`~/.claude/CLAUDE.md`、`~/.codex/AGENTS.md`）に書いてあることはここへ繰り返さない。ここに書くのは、このリポジトリに固有の事情と、共通ルールを上書きする点だけである。

優先順位は「ユーザーの最新指示」→「このファイル」→「共通作業ルール」とする。

## このリポジトリの位置づけ

- ここは複数のPoCを進めるための、コンセプトと仕様だけを追跡するリポジトリである。PoCのコードは置かない。
- 各PoCは独立したGitリポジトリとして作る。親の `.gitignore` へそのディレクトリを1行足してから `git init` する。
- 正本の優先順位は「`CONCEPTS/` → PoC側リポジトリの文書」とする。矛盾したら上位に合わせて下位を直す。
- 現在のPoC: 保護犬猫ポータル（仕様は [CONCEPTS/保護犬猫ポータル.md](CONCEPTS/保護犬猫ポータル.md)）。
- 音声通訳のPoCは `mo` リポジトリ（`~/pj/mo`）へ移した。このリポジトリでは扱わない。

## ドキュメントの置き場所

- 人間向けの詳細仕様は `CONCEPTS/` に置く。第三者が読んで理解できる内容だけを書き、対象はプロダクトの価値と使い方、仕様と設計判断とする。
- 作業履歴、ローカル環境固有のパスや計測値、エージェント自身の作業都合は `CONCEPTS/` に書かない。必要なら `_ai/` へ置く。
- 一時ファイルは `tmp/` に置く。`_ai/` と同じくGit管理しない。
- 完了した作業手順や廃止した設計案の経緯は残さない。現在の仕様を理解するために必要な結論だけを書き、詳細は `git log` に委ねる。
- 現状説明を持つ文書には更新日を置き、内容を変えたら更新日も更新する。
- 新しい文書ファイルは既定で作らない。既存の `CONCEPTS/` 配下の文書か PoC側の `README.md` へ統合できないかを先に確認する。

### 文章の書き方

- 文書本文は日本語で書く。
- 1文の読点（、）は3つまでとする。超える場合は文を分けるか箇条書きにする。
- 箇条書きの1項目・1段落は1トピックに閉じる。性質の異なる内容を1文へ詰め込まない。
- 既に定着した技術用語（API・CLI・UIなど）や固有名詞はそのまま使う。独自の用語を作る場合は、使う前にその文書で定義を与える。

## 実装方針

- 各PoCは無料枠での実行を軸に設計する。無料枠を超える構成を採る場合は、理由と概算費用を先に文書へ残す。
- 外部API・有料API・秘密情報を扱う変更は、導入前に目的と代替案、料金と依存リスク、キー管理を文書へ明記する。
- 課金が発生する検証へ進む前に、ローカルで検証できる範囲を先に潰す。
- 最初から重い構成へ寄せず、まず縦切りで一通り動く経路を通す。

## 検証

- 検証コマンドは各PoCのリポジトリで定義し、そのPoCの `README.md` へ書く。この親リポジトリには現在テスト基盤を置いていない。
- 見えるUI変更では、利用可能なら共通の `$ui-quality` Skillを使う。

## Git運用

- このリポジトリは文書だけを扱うため、`main` のcheckoutで直接作業してよい。共通ルールのbranch・worktree必須はここでは適用しない。mainへ直接コミットした場合は、その回の応答で明示する。
- ブランチ種別に関わらず、原則としてpushしない。必要な場合は作業内容、確認結果、未確認範囲を報告し、ユーザー側で実行してもらう。
- コミットする場合は、可能なら `日本語 / English` の1行形式にする。
- コミット前に `gitleaks git --redact --log-opts='--all' .` で秘密情報を検査する。gitleaksが無い環境ではコミットを黙って通さず停止する。
