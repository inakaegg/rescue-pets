# AGENTS.md — rescue-pets

共通作業ルール（作者のローカル環境にある `~/.claude/CLAUDE.md`、`~/.codex/AGENTS.md`）に書いてあることはここへ繰り返さない。それらが無い環境では、このファイルだけを規則とする。優先順位は「ユーザーの最新指示」→「このファイル」→「共通作業ルール」とする。

## 仕様と文書

- 仕様の正本は [SPEC.md](SPEC.md) とする。実装や他の文書と矛盾したら、仕様に合わせて直す。
- 検証コマンドは [README.md](README.md) の表を正とする。
- 文書本文は日本語で書く。1文の読点（、）は3つまでとする（長文を避けるため）。現状説明を持つ文書には更新日を置く。
- 新しい文書ファイルは既定で作らない。内部メモは `_ai/`、一時ファイルは `tmp/`（どちらもGit管理外）。

## 実装方針

- Amplify Data（AppSync/GraphQL + DynamoDB）は使わない。データは DynamoDB の1テーブルへ REST 経由で読み書きする。ローカルと CLI は同じ保存インターフェースの JSON ファイル実装を使う。
- 外部サービスは AWS だけに限る。Cloudflare、外部のマネージド DB、LINE などを足すときは、先に SPEC の「後回しにする機能」を更新して合意する。
- 機能は CLI を先に実装し、Web UI はその上へ重ねる。最初の縦切りは「クローラ → AI構造化 → 検索」の CLI である。
- 無料枠での実行を軸に設計する。超える構成や秘密情報の導入は、理由・代替案・費用・キー管理を先に文書へ残す。
- LLM（Bedrock）を実際に呼ぶ実行は課金されるため、そのターンの明示許可を必要とする。既定は LLM を呼ばないスタブで動かす。
- `npx ampx sandbox` や deploy などクラウドへ触れる操作は、そのターンの明示許可を必要とする。

## Git運用

- コードまたは製品挙動を変える変更は、共通ルールどおりtask専用branchと別worktreeで行う。文書だけの変更は `main` 直接でよく、直接コミットした場合はその回の応答で明示する。
- 原則としてpushしない。必要な場合は内容を報告し、ユーザー側で実行してもらう。
- コミットは可能なら `日本語 / English` の1行形式にする。
- コミット前に `gitleaks git --redact --log-opts='--all' .` で秘密情報を検査する。gitleaksが無い環境ではコミットを黙って通さず停止する。
